# Interrupts

Interrupts let LangGraph pause for external input or control flow decisions without breaking the execution model.

Use them for human approval, late binding, or any step that must wait on outside state.
