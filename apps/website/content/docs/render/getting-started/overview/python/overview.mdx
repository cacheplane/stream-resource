# Render Overview

Render lets you drive Angular component trees from JSON specifications, so layouts and content can change without redeploying code.

Start here when you want the fastest path to a working Render app in Angular. The product-first guides focus on the core workflow, then connect you to specs, registries, state, and testing as you grow.
