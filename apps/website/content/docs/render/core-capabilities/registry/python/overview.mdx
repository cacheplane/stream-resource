# Registry Overview

The component registry maps spec type strings to Angular component classes so the renderer knows which component to instantiate.

Use this page when you need to register custom components or understand how type resolution works.
