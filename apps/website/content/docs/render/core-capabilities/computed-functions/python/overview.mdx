# Computed Functions Overview

Computed functions derive new values from store state so specs can bind to calculated results instead of raw data.

Use this page when you need to transform or combine state values before they reach a component.
