# Spec Rendering Overview

Spec rendering converts JSON specification objects into live Angular component trees at runtime.

Use this page when you need to understand how render specs map to components and how the rendering pipeline processes them.
