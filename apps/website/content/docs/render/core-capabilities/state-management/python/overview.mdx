# State Management Overview

State management in Render uses a signal-based store that feeds reactive values into spec bindings at runtime.

Use this page when you need to understand how state flows from the store into rendered components.
