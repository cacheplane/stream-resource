# Build Planning Workflows

Build planning flows by defining the task shape, the available tools, and the decision points that control execution.

Keep the first version narrow: one goal, a small tool set, and clear fallbacks for when the plan cannot continue.
