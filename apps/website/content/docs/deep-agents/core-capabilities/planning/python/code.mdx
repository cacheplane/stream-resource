# Planning Code

The code path for planning usually combines a structured prompt, a planning model call, and a runner that executes the resulting steps.

Keep the orchestration small enough that you can inspect the plan, trace the execution, and swap in better tools later.
