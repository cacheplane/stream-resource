# Subagents Overview

Subagents split a larger task into focused pieces with explicit ownership and verification boundaries.

Use this page when a Deep Agents workflow benefits from separate workers instead of a single broad plan.
