# Streaming Code

The code for streaming usually wires the graph runtime to an event handler or UI layer that can consume incremental updates.

Keep the stream adapter small and explicit so you can reason about event order, buffering, and final output separately.
