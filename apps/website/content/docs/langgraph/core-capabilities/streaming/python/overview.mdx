# Streaming Overview

Streaming in LangGraph lets your app surface intermediate progress while the graph is still running.

This guide covers the user-facing behavior of streamed updates and how they fit into a graph that can branch, retry, or pause.
