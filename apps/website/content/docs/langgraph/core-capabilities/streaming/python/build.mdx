# Build Streaming Apps

Build streaming apps by deciding which events matter to the user and which events should stay internal.

Start with a small graph, stream only the outputs that improve the experience, and add more detail once the baseline flow is stable.
