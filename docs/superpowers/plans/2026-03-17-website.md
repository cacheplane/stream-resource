# Next.js Website Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Angular Stream Resource marketing website — landing page, pricing, docs, and live demo — deployed to Vercel.

**Architecture:** Next.js 15 App Router with Tailwind CSS dark luxury design system, Framer Motion scroll-triggered animations, SVG architecture diagram with `animateMotion`, MDX docs with Claude-generated narrative content, and an embedded Angular Elements live demo.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS, Framer Motion, `next-mdx-remote`, `shiki`, TypeDoc, Claude API (`claude-sonnet-4-6`), Playwright, Vercel, Angular Elements, Nx

---

## File Map

```
apps/website/
├── app/
│   ├── layout.tsx                  # Root layout: fonts, global CSS, Nav/Footer
│   ├── globals.css                 # CSS custom properties + Tailwind base
│   ├── page.tsx                    # Landing page
│   ├── pricing/page.tsx            # Pricing page
│   ├── docs/[[...slug]]/page.tsx   # MDX narrative docs
│   ├── api-reference/page.tsx      # TypeDoc API reference
│   └── api/leads/route.ts          # Lead gen POST handler (JSON)
├── components/
│   ├── landing/
│   │   ├── Hero.tsx                # 156px EB Garamond headline, Framer Motion
│   │   ├── ArchDiagram.tsx         # SVG animateMotion architecture animation
│   │   ├── FeatureStrip.tsx        # 6-card feature grid
│   │   └── CodeBlock.tsx           # shiki syntax-highlighted code
│   ├── pricing/
│   │   ├── PricingGrid.tsx         # 3-plan cards + deployment add-on card
│   │   ├── CompareTable.tsx        # Feature comparison table
│   │   └── LeadForm.tsx            # Enterprise lead gen form
│   ├── docs/
│   │   ├── DocsSidebar.tsx         # Navigation tree sidebar
│   │   ├── MdxRenderer.tsx         # next-mdx-remote wrapper
│   │   └── ApiRefTable.tsx         # TypeDoc API reference renderer
│   └── shared/
│       ├── Nav.tsx                 # Top navigation bar
│       ├── Footer.tsx              # Site footer
│       └── InstallStrip.tsx        # npm install command + copy button
├── lib/
│   ├── design-tokens.ts            # Typed design constants
│   └── docs.ts                     # MDX content loader helpers
├── content/docs/                   # Claude-generated MDX (committed after generation)
├── scripts/
│   ├── generate-api-docs.ts        # TypeDoc → api-docs.json
│   └── generate-narrative-docs.ts  # api-docs.json + source → Claude API → MDX
├── public/
│   └── demo/                       # Angular Elements bundle (built by apps/demo)
├── e2e/                            # Playwright specs
├── tailwind.config.ts
├── next.config.ts
└── vercel.json
apps/demo/                          # Angular Elements demo app
├── src/app/
│   └── chat-demo/
│       ├── chat-demo.component.ts  # Web component using streamResource()
│       └── chat-demo.module.ts
└── project.json
```

---

## Task 1: Nx Next.js App Scaffold

**Files:**
- Create: `apps/website/` (generated by Nx)
- Create: `apps/website/next.config.ts`
- Create: `apps/website/tailwind.config.ts`

- [ ] **Step 1: Generate Next.js app within existing Nx monorepo**

```bash
cd /path/to/stream-resource
npx nx g @nx/next:app website \
  --directory=apps/website \
  --style=css \
  --appRouter=true \
  --unitTestRunner=none \
  --e2eTestRunner=none \
  --no-interactive
```

Expected: `apps/website/` created with `app/`, `public/`, `next.config.ts`, `package.json`

- [ ] **Step 2: Install dependencies**

```bash
npm install framer-motion next-mdx-remote shiki @anthropic-ai/sdk typedoc
npm install -D @playwright/test
```

- [ ] **Step 3: Verify Next.js builds**

```bash
npx nx build website
```

Expected: Build succeeds, `.next/` created under `apps/website/`

- [ ] **Step 4: Commit**

```bash
git add apps/website/
git commit -m "chore: scaffold Next.js website app in Nx monorepo"
```

---

## Task 2: Design System

**Files:**
- Modify: `apps/website/tailwind.config.ts`
- Create: `apps/website/app/globals.css`
- Create: `apps/website/lib/design-tokens.ts`

- [ ] **Step 1: Write design tokens**

Create `apps/website/lib/design-tokens.ts`:

```typescript
export const colors = {
  bg: '#080705',
  gold: '#d4aa6a',
  goldFaint: 'rgba(212,170,106,0.12)',
  textPrimary: '#f4efe6',
  textSecondary: '#c8b898',
  textMuted: '#7a6e5e',
} as const;

export const fonts = {
  heading: 'var(--font-garamond)',
  mono: 'var(--font-mono)',
  body: 'var(--font-inter)',
} as const;
```

- [ ] **Step 2: Configure Tailwind**

Replace `apps/website/tailwind.config.ts`:

```typescript
import type { Config } from 'tailwindcss';

export default {
  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        bg: '#080705',
        gold: '#d4aa6a',
        'gold-faint': 'rgba(212,170,106,0.12)',
        'text-primary': '#f4efe6',
        'text-secondary': '#c8b898',
        'text-muted': '#7a6e5e',
      },
      fontFamily: {
        heading: ['var(--font-garamond)', 'Georgia', 'serif'],
        mono: ['var(--font-mono)', 'monospace'],
        body: ['var(--font-inter)', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
} satisfies Config;
```

- [ ] **Step 3: Write global CSS**

Create `apps/website/app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --font-garamond: 'EB Garamond', Georgia, serif;
  --font-mono: 'JetBrains Mono', monospace;
  --font-inter: 'Inter', system-ui, sans-serif;
}

html {
  background: #080705;
  color: #f4efe6;
}

body {
  font-family: var(--font-inter);
  font-size: 15px;
  line-height: 1.6;
}

/* Gold border utility */
.border-gold { border-color: rgba(212, 170, 106, 0.3); }
.border-gold-faint { border-color: rgba(212, 170, 106, 0.12); }
```

- [ ] **Step 4: Verify build passes**

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 5: Commit**

```bash
git add apps/website/tailwind.config.ts apps/website/app/globals.css apps/website/lib/design-tokens.ts
git commit -m "feat(website): add dark luxury design system tokens"
```

---

## Task 3: Root Layout + Shared Nav/Footer

**Files:**
- Create: `apps/website/app/layout.tsx`
- Create: `apps/website/components/shared/Nav.tsx`
- Create: `apps/website/components/shared/Footer.tsx`
- Create: `apps/website/components/shared/InstallStrip.tsx`

- [ ] **Step 1: Create Nav component**

Create `apps/website/components/shared/Nav.tsx`:

```tsx
'use client';
import Link from 'next/link';

export function Nav() {
  return (
    <nav className="fixed top-0 left-0 right-0 z-50 border-b border-gold-faint bg-bg/90 backdrop-blur-sm">
      <div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">
        <Link href="/" className="font-heading text-xl font-bold text-gold">
          stream-resource
        </Link>
        <div className="flex items-center gap-8">
          <Link href="/docs" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
            Docs
          </Link>
          <Link href="/api-reference" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
            API
          </Link>
          <Link href="/pricing" className="text-sm text-text-secondary hover:text-text-primary transition-colors">
            Pricing
          </Link>
          <a
            href="https://github.com/your-org/stream-resource"
            className="rounded border border-gold/30 px-4 py-1.5 text-sm text-gold hover:bg-gold/10 transition-colors"
          >
            GitHub
          </a>
        </div>
      </div>
    </nav>
  );
}
```

- [ ] **Step 2: Create Footer component**

Create `apps/website/components/shared/Footer.tsx`:

```tsx
import Link from 'next/link';

export function Footer() {
  return (
    <footer className="border-t border-gold-faint mt-32 py-16">
      <div className="mx-auto max-w-7xl px-6">
        <div className="flex flex-col items-center gap-6 text-center">
          <p className="font-heading text-lg text-gold italic">
            The Enterprise Streaming Resource for LangChain and Angular
          </p>
          <div className="flex gap-8 text-sm text-text-muted">
            <Link href="/docs" className="hover:text-text-secondary transition-colors">Docs</Link>
            <Link href="/pricing" className="hover:text-text-secondary transition-colors">Pricing</Link>
            <Link href="/api-reference" className="hover:text-text-secondary transition-colors">API Reference</Link>
            <a href="https://github.com/your-org/stream-resource" className="hover:text-text-secondary transition-colors">GitHub</a>
          </div>
          <p className="text-xs text-text-muted">
            © {new Date().getFullYear()} Angular Stream Resource.{' '}
            <Link href="/pricing" className="hover:text-text-secondary">Commercial use requires a license.</Link>
          </p>
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 3: Create InstallStrip component**

Create `apps/website/components/shared/InstallStrip.tsx`:

```tsx
'use client';
import { useState } from 'react';

export function InstallStrip() {
  const [copied, setCopied] = useState(false);
  const cmd = 'npm install stream-resource';

  const copy = () => {
    navigator.clipboard.writeText(cmd);
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };

  return (
    <div className="flex items-center gap-4 rounded border border-gold-faint bg-white/5 px-6 py-3">
      <span className="font-mono text-sm text-gold">$</span>
      <code className="font-mono text-sm text-text-secondary flex-1">{cmd}</code>
      <button
        onClick={copy}
        className="text-xs text-text-muted hover:text-gold transition-colors"
        aria-label="Copy install command"
      >
        {copied ? 'Copied!' : 'Copy'}
      </button>
    </div>
  );
}
```

- [ ] **Step 4: Create root layout**

Create `apps/website/app/layout.tsx`:

```tsx
import type { Metadata } from 'next';
import { EB_Garamond, Inter, JetBrains_Mono } from 'next/font/google';
import './globals.css';
import { Nav } from '../components/shared/Nav';
import { Footer } from '../components/shared/Footer';

const garamond = EB_Garamond({
  subsets: ['latin'],
  variable: '--font-garamond',
  weight: ['400', '500', '700', '800'],
  style: ['normal', 'italic'],
});

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
});

const mono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
});

export const metadata: Metadata = {
  title: 'stream-resource — Enterprise Angular Streaming for LangChain',
  description: 'The Enterprise Streaming Resource for LangChain and Angular. Full-parity implementation of LangGraph\'s useStream() for Angular 20+.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${garamond.variable} ${inter.variable} ${mono.variable}`}>
      <body className="bg-bg text-text-primary">
        <Nav />
        <main className="pt-20">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
```

- [ ] **Step 5: Verify build**

```bash
npx nx build website
```

Expected: Build succeeds with no type errors

- [ ] **Step 6: Commit**

```bash
git add apps/website/app/layout.tsx apps/website/components/shared/
git commit -m "feat(website): add root layout, Nav, Footer, InstallStrip"
```

---

## Task 4: Hero Component

**Files:**
- Create: `apps/website/components/landing/Hero.tsx`

- [ ] **Step 1: Write Hero component**

Create `apps/website/components/landing/Hero.tsx`:

```tsx
'use client';
import { motion } from 'framer-motion';
import { InstallStrip } from '../shared/InstallStrip';

const fadeUp = {
  hidden: { opacity: 0, y: 32 },
  visible: (i: number) => ({
    opacity: 1,
    y: 0,
    transition: { delay: i * 0.12, duration: 0.7, ease: [0.22, 1, 0.36, 1] },
  }),
};

export function Hero() {
  return (
    <section className="relative min-h-screen flex flex-col items-center justify-center px-6 pb-24 overflow-hidden">
      {/* Subtle gold radial glow */}
      <div
        className="pointer-events-none absolute inset-0"
        style={{
          background: 'radial-gradient(ellipse 60% 40% at 50% 60%, rgba(212,170,106,0.06) 0%, transparent 70%)',
        }}
      />

      <div className="relative z-10 max-w-5xl w-full text-center">
        {/* Eyebrow */}
        <motion.p
          custom={0}
          initial="hidden"
          animate="visible"
          variants={fadeUp}
          className="font-mono text-xs uppercase tracking-widest text-gold mb-8"
        >
          Angular 20+ · LangGraph · RxJS
        </motion.p>

        {/* Main headline */}
        <motion.h1
          custom={1}
          initial="hidden"
          animate="visible"
          variants={fadeUp}
          className="font-heading font-extrabold text-text-primary leading-[0.92] mb-6"
          style={{ fontSize: 'clamp(72px, 10vw, 156px)' }}
        >
          stream
          <br />
          <span className="text-gold italic">resource</span>
        </motion.h1>

        {/* Tagline */}
        <motion.p
          custom={2}
          initial="hidden"
          animate="visible"
          variants={fadeUp}
          className="font-heading italic text-text-secondary mb-12"
          style={{ fontSize: 'clamp(18px, 2vw, 28px)' }}
        >
          The Enterprise Streaming Resource for LangChain and Angular
        </motion.p>

        {/* Install strip */}
        <motion.div
          custom={3}
          initial="hidden"
          animate="visible"
          variants={fadeUp}
          className="flex justify-center mb-8"
        >
          <div className="w-full max-w-md">
            <InstallStrip />
          </div>
        </motion.div>

        {/* CTA buttons */}
        <motion.div
          custom={4}
          initial="hidden"
          animate="visible"
          variants={fadeUp}
          className="flex gap-4 justify-center flex-wrap"
        >
          <a
            href="/docs"
            className="rounded px-8 py-3 text-sm font-medium bg-gold text-bg hover:bg-gold/90 transition-colors"
          >
            Get Started
          </a>
          <a
            href="/pricing"
            className="rounded border border-gold/30 px-8 py-3 text-sm text-gold hover:bg-gold/10 transition-colors"
          >
            See Pricing
          </a>
        </motion.div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Verify Hero renders (quick smoke test)**

Create `apps/website/app/page.tsx` (temporary — replaced in Task 8):

```tsx
import { Hero } from '../components/landing/Hero';

export default function Home() {
  return <Hero />;
}
```

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 3: Commit**

```bash
git add apps/website/components/landing/Hero.tsx apps/website/app/page.tsx
git commit -m "feat(website): add Hero component with Framer Motion animations"
```

---

## Task 5: Architecture Diagram SVG Animation

**Files:**
- Create: `apps/website/components/landing/ArchDiagram.tsx`

- [ ] **Step 1: Write ArchDiagram component**

Create `apps/website/components/landing/ArchDiagram.tsx`:

```tsx
'use client';
import { motion, useInView } from 'framer-motion';
import { useRef } from 'react';

const GOLD = '#d4aa6a';
const GOLD_FAINT = 'rgba(212,170,106,0.12)';
const TEXT = '#f4efe6';
const MUTED = '#7a6e5e';

const nodes = [
  { id: 'angular',   x: 60,  y: 50, label: 'Angular\nComponent',  sub: 'consumer' },
  { id: 'sr',        x: 280, y: 50, label: 'streamResource()',     sub: 'Angular Signals' },
  { id: 'bridge',    x: 500, y: 50, label: 'StreamManager\nBridge', sub: 'RxJS BehaviorSubjects' },
  { id: 'langgraph', x: 720, y: 50, label: 'LangGraph\nPlatform',  sub: 'SSE Stream' },
];

const arrows = [
  { from: [160, 70], to: [280, 70], label: 'submit()' },
  { from: [455, 70], to: [500, 70], label: '' },
  { from: [680, 70], to: [720, 70], label: '' },
];

export function ArchDiagram() {
  const ref = useRef<SVGSVGElement>(null);
  const inView = useInView(ref, { once: true, amount: 0.4 });

  return (
    <section className="mx-auto max-w-5xl px-6 py-24">
      <motion.p
        initial={{ opacity: 0 }}
        animate={inView ? { opacity: 1 } : {}}
        transition={{ duration: 0.6 }}
        className="font-mono text-xs uppercase tracking-widest text-gold text-center mb-12"
      >
        Architecture
      </motion.p>

      <svg
        ref={ref}
        viewBox="0 0 900 200"
        className="w-full"
        aria-label="Angular Stream Resource architecture diagram"
      >
        <defs>
          <marker id="arrowhead" markerWidth="8" markerHeight="8" refX="8" refY="4" orient="auto">
            <path d="M0,0 L8,4 L0,8 Z" fill={GOLD} opacity="0.6" />
          </marker>
          {/* Animated particle path — forward */}
          <path id="flow-path" d="M 160,100 L 840,100" />
        </defs>

        {/* Connection lines */}
        <line x1="160" y1="100" x2="280" y2="100" stroke={GOLD} strokeOpacity="0.3" strokeWidth="1.5" markerEnd="url(#arrowhead)" />
        <line x1="420" y1="100" x2="500" y2="100" stroke={GOLD} strokeOpacity="0.3" strokeWidth="1.5" markerEnd="url(#arrowhead)" />
        <line x1="640" y1="100" x2="720" y2="100" stroke={GOLD} strokeOpacity="0.3" strokeWidth="1.5" markerEnd="url(#arrowhead)" />

        {/* Return path (dashed) — signals flowing back */}
        <line x1="720" y1="120" x2="160" y2="120" stroke={MUTED} strokeOpacity="0.4" strokeWidth="1" strokeDasharray="4 4" />

        {/* Nodes */}
        {nodes.map((node, i) => (
          <motion.g
            key={node.id}
            initial={{ opacity: 0, y: 8 }}
            animate={inView ? { opacity: 1, y: 0 } : {}}
            transition={{ delay: i * 0.15, duration: 0.5 }}
          >
            <rect
              x={node.x}
              y={75}
              width={140}
              height={50}
              rx={6}
              fill={GOLD_FAINT}
              stroke={GOLD}
              strokeOpacity="0.25"
              strokeWidth="1"
            />
            <text
              x={node.x + 70}
              y={96}
              textAnchor="middle"
              fill={TEXT}
              fontSize="11"
              fontFamily="var(--font-mono)"
              fontWeight="600"
            >
              {node.label.split('\n')[0]}
            </text>
            {node.label.includes('\n') && (
              <text x={node.x + 70} y={109} textAnchor="middle" fill={TEXT} fontSize="11" fontFamily="var(--font-mono)" fontWeight="600">
                {node.label.split('\n')[1]}
              </text>
            )}
            <text
              x={node.x + 70}
              y={node.label.includes('\n') ? 122 : 112}
              textAnchor="middle"
              fill={MUTED}
              fontSize="9"
              fontFamily="var(--font-inter)"
            >
              {node.sub}
            </text>
          </motion.g>
        ))}

        {/* Animated stream particle */}
        {inView && (
          <circle r="4" fill={GOLD} opacity="0.8">
            <animateMotion
              dur="2.5s"
              repeatCount="indefinite"
              path="M 160,100 L 300,100 L 440,100 L 580,100 L 720,100"
              calcMode="linear"
            />
          </circle>
        )}

        {/* Labels */}
        <text x={220} y={68} textAnchor="middle" fill={MUTED} fontSize="9" fontFamily="var(--font-inter)">submit()</text>
        <text x={460} y={68} textAnchor="middle" fill={MUTED} fontSize="9" fontFamily="var(--font-inter)">bridge</text>
        <text x={680} y={68} textAnchor="middle" fill={MUTED} fontSize="9" fontFamily="var(--font-inter)">SSE</text>
        <text x={440} y={140} textAnchor="middle" fill={MUTED} fontSize="9" fontFamily="var(--font-inter)" fontStyle="italic">Angular Signals flow back →</text>
      </svg>
    </section>
  );
}
```

- [ ] **Step 2: Verify build**

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 3: Commit**

```bash
git add apps/website/components/landing/ArchDiagram.tsx
git commit -m "feat(website): add SVG architecture diagram with animateMotion"
```

---

## Task 6: FeatureStrip + CodeBlock

**Files:**
- Create: `apps/website/components/landing/FeatureStrip.tsx`
- Create: `apps/website/components/landing/CodeBlock.tsx`

- [ ] **Step 1: Create CodeBlock component**

Create `apps/website/components/landing/CodeBlock.tsx`:

```tsx
import { codeToHtml } from 'shiki';

interface CodeBlockProps {
  code: string;
  lang?: string;
  title?: string;
}

export async function CodeBlock({ code, lang = 'typescript', title }: CodeBlockProps) {
  const html = await codeToHtml(code, {
    lang,
    theme: 'github-dark',
  });

  return (
    <div className="rounded border border-gold-faint overflow-hidden">
      {title && (
        <div className="border-b border-gold-faint px-4 py-2 bg-white/5">
          <span className="font-mono text-xs text-text-muted">{title}</span>
        </div>
      )}
      <div
        className="p-4 overflow-x-auto text-sm"
        dangerouslySetInnerHTML={{ __html: html }}
      />
    </div>
  );
}
```

- [ ] **Step 2: Create FeatureStrip component**

Create `apps/website/components/landing/FeatureStrip.tsx`:

```tsx
'use client';
import { motion, useInView } from 'framer-motion';
import { useRef } from 'react';

const features = [
  {
    title: 'Angular Signals',
    desc: 'Every stream event exposed as an Angular Signal. Full reactivity out of the box.',
  },
  {
    title: 'Full Parity',
    desc: 'Matches LangGraph\'s useStream() API — same options, same state shape, same lifecycle.',
  },
  {
    title: 'RxJS Core',
    desc: 'BehaviorSubject pipeline handles rapid stream events without dropping updates.',
  },
  {
    title: 'MockStreamTransport',
    desc: 'Ship deterministic unit tests without a running LangGraph server.',
  },
  {
    title: 'Enterprise Ready',
    desc: 'Custom transport, onThreadId callbacks, message filtering, throttle control.',
  },
  {
    title: 'TypeScript First',
    desc: 'Full type inference from your LangGraph agent via BagTemplate and InferBag.',
  },
];

export function FeatureStrip() {
  const ref = useRef<HTMLDivElement>(null);
  const inView = useInView(ref, { once: true, amount: 0.2 });

  return (
    <section ref={ref} className="mx-auto max-w-5xl px-6 py-24">
      <motion.h2
        initial={{ opacity: 0, y: 24 }}
        animate={inView ? { opacity: 1, y: 0 } : {}}
        transition={{ duration: 0.6 }}
        className="font-heading font-extrabold text-center mb-16"
        style={{ fontSize: 'clamp(36px, 5vw, 60px)' }}
      >
        Built for{' '}
        <span className="text-gold italic">enterprise</span>
      </motion.h2>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {features.map((f, i) => (
          <motion.div
            key={f.title}
            initial={{ opacity: 0, y: 20 }}
            animate={inView ? { opacity: 1, y: 0 } : {}}
            transition={{ delay: i * 0.08, duration: 0.5 }}
            className="rounded border border-gold-faint p-6 hover:border-gold/30 transition-colors"
          >
            <h3 className="font-heading font-bold text-gold mb-2 text-lg">{f.title}</h3>
            <p className="text-text-secondary text-sm leading-relaxed">{f.desc}</p>
          </motion.div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 3: Verify build**

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 4: Commit**

```bash
git add apps/website/components/landing/FeatureStrip.tsx apps/website/components/landing/CodeBlock.tsx
git commit -m "feat(website): add FeatureStrip and CodeBlock components"
```

---

## Task 7: Landing Page Assembly

**Files:**
- Modify: `apps/website/app/page.tsx`

- [ ] **Step 1: Assemble landing page**

Replace `apps/website/app/page.tsx`:

```tsx
import { Hero } from '../components/landing/Hero';
import { ArchDiagram } from '../components/landing/ArchDiagram';
import { FeatureStrip } from '../components/landing/FeatureStrip';
import { CodeBlock } from '../components/landing/CodeBlock';

const DEMO_CODE = `import { Component, inject } from '@angular/core';
import { streamResource, provideStreamResource } from 'stream-resource';

@Component({
  selector: 'app-chat',
  providers: [provideStreamResource({ apiUrl: 'https://your-langgraph-platform.app' })],
  template: \`
    @if (stream.isLoading()) { <p>Thinking…</p> }
    @for (msg of stream.messages(); track msg.id) {
      <div>{{ msg.content }}</div>
    }
    <button (click)="send()">Send</button>
  \`,
})
export class ChatComponent {
  stream = streamResource<{ messages: BaseMessage[] }>({
    apiUrl: 'https://your-langgraph-platform.app',
    assistantId: 'my-agent',
  });

  send() {
    this.stream.submit({ messages: [{ role: 'human', content: 'Hello' }] });
  }
}`;

export default function Home() {
  return (
    <>
      <Hero />
      <ArchDiagram />
      <section className="mx-auto max-w-3xl px-6 py-12">
        <h2 className="font-heading font-extrabold text-center mb-8" style={{ fontSize: 'clamp(28px, 4vw, 48px)' }}>
          30 seconds to{' '}
          <span className="text-gold italic">streaming</span>
        </h2>
        <CodeBlock code={DEMO_CODE} lang="typescript" title="app/chat.component.ts" />
      </section>
      <FeatureStrip />
    </>
  );
}
```

- [ ] **Step 2: Verify build**

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 3: Commit**

```bash
git add apps/website/app/page.tsx
git commit -m "feat(website): assemble landing page (Hero, ArchDiagram, CodeBlock, FeatureStrip)"
```

---

## Task 8: Pricing Page

**Files:**
- Create: `apps/website/components/pricing/PricingGrid.tsx`
- Create: `apps/website/components/pricing/CompareTable.tsx`
- Create: `apps/website/components/pricing/LeadForm.tsx`
- Create: `apps/website/app/pricing/page.tsx`

- [ ] **Step 1: Create PricingGrid component**

Create `apps/website/components/pricing/PricingGrid.tsx`:

```tsx
'use client';

const plans = [
  {
    name: 'Open Source',
    price: 'Free',
    period: 'forever',
    desc: 'Non-commercial use. Personal projects, open source, academic.',
    features: ['Full library access', 'Community support', 'MIT license', 'Non-commercial only'],
    cta: 'Download',
    ctaHref: 'https://github.com/your-org/stream-resource',
    highlight: false,
  },
  {
    name: 'Developer Seat',
    price: '$500',
    period: 'per seat / year',
    desc: 'Commercial use. One developer, all projects for 12 months.',
    features: ['Commercial license', 'Email support', '12-month release window', 'Transferable seat'],
    cta: 'Buy a Seat',
    ctaHref: '#enterprise',
    highlight: true,
  },
  {
    name: 'App Deployment',
    price: '$2,000',
    period: 'per application',
    desc: 'One-time per application. Covers dev, staging, and production.',
    features: ['Per-application license', 'All environments', 'Perpetual for purchased version', 'No seat limit'],
    cta: 'License an App',
    ctaHref: '#enterprise',
    highlight: false,
  },
];

export function PricingGrid() {
  return (
    <section className="mx-auto max-w-5xl px-6 py-24">
      <h1 className="font-heading font-extrabold text-center mb-4" style={{ fontSize: 'clamp(48px, 6vw, 80px)' }}>
        Simple <span className="text-gold italic">pricing</span>
      </h1>
      <p className="text-center text-text-secondary mb-16 text-lg">
        Free for non-commercial use. License when you ship.
      </p>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
        {plans.map((plan) => (
          <div
            key={plan.name}
            className={`rounded border p-8 flex flex-col ${
              plan.highlight
                ? 'border-gold/50 bg-gold/5'
                : 'border-gold-faint'
            }`}
          >
            {plan.highlight && (
              <span className="font-mono text-xs uppercase tracking-widest text-gold mb-4">Most Popular</span>
            )}
            <h2 className="font-heading font-bold text-xl mb-2">{plan.name}</h2>
            <div className="mb-2">
              <span className="font-heading font-extrabold text-4xl text-gold">{plan.price}</span>
              <span className="text-text-muted text-sm ml-2">{plan.period}</span>
            </div>
            <p className="text-text-secondary text-sm mb-6">{plan.desc}</p>
            <ul className="space-y-2 mb-8 flex-1">
              {plan.features.map((f) => (
                <li key={f} className="text-sm text-text-secondary flex gap-2">
                  <span className="text-gold">✓</span> {f}
                </li>
              ))}
            </ul>
            <a
              href={plan.ctaHref}
              className={`rounded py-3 text-center text-sm font-medium transition-colors ${
                plan.highlight
                  ? 'bg-gold text-bg hover:bg-gold/90'
                  : 'border border-gold/30 text-gold hover:bg-gold/10'
              }`}
            >
              {plan.cta}
            </a>
          </div>
        ))}
      </div>

      {/* Enterprise add-on */}
      <div className="rounded border border-gold-faint p-8 flex flex-col md:flex-row items-center justify-between gap-6">
        <div>
          <h3 className="font-heading font-bold text-xl mb-1">Enterprise</h3>
          <p className="text-text-secondary text-sm">Volume seats, unlimited deployments, SLA, dedicated support, custom contract.</p>
        </div>
        <a href="#enterprise" className="whitespace-nowrap rounded border border-gold/30 px-8 py-3 text-sm text-gold hover:bg-gold/10 transition-colors">
          Contact Sales
        </a>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Create LeadForm component**

Create `apps/website/components/pricing/LeadForm.tsx`:

```tsx
'use client';
import { useState } from 'react';

export function LeadForm() {
  const [sent, setSent] = useState(false);
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setLoading(true);
    const data = Object.fromEntries(new FormData(e.currentTarget));
    await fetch('/api/leads', { method: 'POST', body: JSON.stringify(data), headers: { 'Content-Type': 'application/json' } });
    setSent(true);
    setLoading(false);
  }

  if (sent) {
    return (
      <div className="rounded border border-gold/30 p-8 text-center">
        <p className="font-heading text-xl text-gold mb-2">Thank you.</p>
        <p className="text-text-secondary text-sm">We'll be in touch shortly.</p>
      </div>
    );
  }

  return (
    <form onSubmit={handleSubmit} id="enterprise" className="rounded border border-gold-faint p-8 space-y-4">
      <h3 className="font-heading font-bold text-2xl mb-4">Enterprise inquiry</h3>
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        <input required name="name" placeholder="Name" className="rounded border border-gold-faint bg-white/5 px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:border-gold/50 outline-none" />
        <input required name="email" type="email" placeholder="Work email" className="rounded border border-gold-faint bg-white/5 px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:border-gold/50 outline-none" />
      </div>
      <input name="company" placeholder="Company" className="w-full rounded border border-gold-faint bg-white/5 px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:border-gold/50 outline-none" />
      <textarea name="message" placeholder="Tell us about your use case" rows={4} className="w-full rounded border border-gold-faint bg-white/5 px-4 py-3 text-sm text-text-primary placeholder:text-text-muted focus:border-gold/50 outline-none resize-none" />
      <button type="submit" disabled={loading} className="rounded bg-gold px-8 py-3 text-sm font-medium text-bg hover:bg-gold/90 disabled:opacity-50 transition-colors">
        {loading ? 'Sending…' : 'Get in touch'}
      </button>
    </form>
  );
}
```

- [ ] **Step 3: Create CompareTable component**

Create `apps/website/components/pricing/CompareTable.tsx`:

```tsx
const rows = [
  ['Full Signal API', '✓', '✓', '✓'],
  ['LangGraph Platform', '✓', '✓', '✓'],
  ['MockStreamTransport', '✓', '✓', '✓'],
  ['Commercial use', '✗', '✓', '✓'],
  ['Email support', '✗', '✓', '✓'],
  ['SLA', '✗', '✗', '✓'],
  ['Volume pricing', '✗', '✗', '✓'],
];

export function CompareTable() {
  return (
    <section className="mx-auto max-w-3xl px-6 py-12">
      <h2 className="font-heading font-bold text-2xl text-center mb-8">Compare plans</h2>
      <table className="w-full text-sm">
        <thead>
          <tr className="border-b border-gold-faint">
            <th className="text-left py-3 text-text-muted font-normal">Feature</th>
            <th className="text-center py-3 text-text-secondary font-medium">Open Source</th>
            <th className="text-center py-3 text-gold font-medium">Developer</th>
            <th className="text-center py-3 text-text-secondary font-medium">Enterprise</th>
          </tr>
        </thead>
        <tbody>
          {rows.map(([label, ...cols]) => (
            <tr key={label as string} className="border-b border-gold-faint/50">
              <td className="py-3 text-text-secondary">{label}</td>
              {cols.map((v, i) => (
                <td key={i} className={`text-center py-3 ${v === '✓' ? 'text-gold' : 'text-text-muted'}`}>{v}</td>
              ))}
            </tr>
          ))}
        </tbody>
      </table>
    </section>
  );
}
```

- [ ] **Step 4: Create pricing page**

Create `apps/website/app/pricing/page.tsx`:

```tsx
import { PricingGrid } from '../../components/pricing/PricingGrid';
import { CompareTable } from '../../components/pricing/CompareTable';
import { LeadForm } from '../../components/pricing/LeadForm';

export const metadata = { title: 'Pricing — stream-resource' };

export default function PricingPage() {
  return (
    <>
      <PricingGrid />
      <CompareTable />
      <div className="mx-auto max-w-3xl px-6 py-12">
        <LeadForm />
      </div>
    </>
  );
}
```

- [ ] **Step 5: Create lead gen API route**

Create `apps/website/app/api/leads/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server';

export async function POST(req: NextRequest) {
  const body = await req.json();
  const { name, email, company, message } = body;

  if (!name || !email) {
    return NextResponse.json({ error: 'name and email required' }, { status: 400 });
  }

  // Log to console for now — replace with CRM/email service in production
  console.log('[lead]', { name, email, company, message, ts: new Date().toISOString() });

  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 6: Verify build**

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 7: Commit**

```bash
git add apps/website/components/pricing/ apps/website/app/pricing/ apps/website/app/api/
git commit -m "feat(website): add pricing page, comparison table, and lead gen form"
```

---

## Task 9: Docs Infrastructure

**Files:**
- Create: `apps/website/lib/docs.ts`
- Create: `apps/website/components/docs/MdxRenderer.tsx`
- Create: `apps/website/components/docs/DocsSidebar.tsx`
- Create: `apps/website/app/docs/[[...slug]]/page.tsx`
- Create: `apps/website/content/docs/index.mdx` (placeholder)

- [ ] **Step 1: Create docs lib helper**

Create `apps/website/lib/docs.ts`:

```typescript
import fs from 'fs';
import path from 'path';

const DOCS_DIR = path.join(process.cwd(), 'content/docs');

export interface DocEntry {
  slug: string[];
  title: string;
}

export function getAllDocs(): DocEntry[] {
  if (!fs.existsSync(DOCS_DIR)) return [];
  return fs
    .readdirSync(DOCS_DIR)
    .filter((f) => f.endsWith('.mdx'))
    .map((f) => ({
      slug: [f.replace(/\.mdx$/, '')],
      title: f.replace(/\.mdx$/, '').replace(/-/g, ' '),
    }));
}

export async function getDoc(slug: string[]): Promise<{ content: string; title: string } | null> {
  const filePath = path.join(DOCS_DIR, `${slug.join('/')}.mdx`);
  if (!fs.existsSync(filePath)) return null;
  const content = fs.readFileSync(filePath, 'utf-8');
  return { content, title: slug[slug.length - 1].replace(/-/g, ' ') };
}
```

- [ ] **Step 2: Create MdxRenderer**

Create `apps/website/components/docs/MdxRenderer.tsx`:

```tsx
import { MDXRemote } from 'next-mdx-remote/rsc';
import { CodeBlock } from '../landing/CodeBlock';

const components = {
  pre: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  code: ({ className, children }: { className?: string; children?: string }) => {
    const lang = className?.replace('language-', '') ?? 'text';
    if (children?.includes('\n')) {
      return <CodeBlock code={children.trim()} lang={lang} />;
    }
    return <code className="font-mono text-sm bg-white/10 px-1.5 py-0.5 rounded text-gold">{children}</code>;
  },
  h1: (p: React.HTMLAttributes<HTMLHeadingElement>) => <h1 className="font-heading font-extrabold text-4xl mb-6" {...p} />,
  h2: (p: React.HTMLAttributes<HTMLHeadingElement>) => <h2 className="font-heading font-bold text-2xl mt-12 mb-4" {...p} />,
  h3: (p: React.HTMLAttributes<HTMLHeadingElement>) => <h3 className="font-heading font-semibold text-xl mt-8 mb-3" {...p} />,
  p:  (p: React.HTMLAttributes<HTMLParagraphElement>) => <p className="text-text-secondary leading-relaxed mb-4" {...p} />,
  ul: (p: React.HTMLAttributes<HTMLUListElement>) => <ul className="list-disc list-inside text-text-secondary space-y-1 mb-4" {...p} />,
};

export function MdxRenderer({ content }: { content: string }) {
  return (
    <div className="prose-custom">
      <MDXRemote source={content} components={components} />
    </div>
  );
}
```

- [ ] **Step 3: Create DocsSidebar**

Create `apps/website/components/docs/DocsSidebar.tsx`:

```tsx
import Link from 'next/link';
import { getAllDocs } from '../../lib/docs';

export function DocsSidebar() {
  const docs = getAllDocs();
  return (
    <nav className="w-56 shrink-0">
      <p className="font-mono text-xs uppercase tracking-widest text-gold mb-4">Docs</p>
      <ul className="space-y-1">
        {docs.map((doc) => (
          <li key={doc.slug.join('/')}>
            <Link
              href={`/docs/${doc.slug.join('/')}`}
              className="block text-sm text-text-secondary hover:text-text-primary transition-colors py-1 capitalize"
            >
              {doc.title}
            </Link>
          </li>
        ))}
      </ul>
    </nav>
  );
}
```

- [ ] **Step 4: Create docs page**

Create `apps/website/app/docs/[[...slug]]/page.tsx`:

```tsx
import { notFound } from 'next/navigation';
import { getAllDocs, getDoc } from '../../../lib/docs';
import { MdxRenderer } from '../../../components/docs/MdxRenderer';
import { DocsSidebar } from '../../../components/docs/DocsSidebar';

interface Props {
  params: Promise<{ slug?: string[] }>;
}

export async function generateStaticParams() {
  const docs = getAllDocs();
  return [{ slug: undefined }, ...docs.map((d) => ({ slug: d.slug }))];
}

export default async function DocsPage({ params }: Props) {
  const { slug } = await params;
  const effectiveSlug = slug ?? ['index'];
  const doc = await getDoc(effectiveSlug);
  if (!doc) notFound();

  return (
    <div className="mx-auto max-w-6xl px-6 py-16 flex gap-12">
      <DocsSidebar />
      <article className="flex-1 min-w-0">
        <MdxRenderer content={doc.content} />
      </article>
    </div>
  );
}
```

- [ ] **Step 5: Create placeholder index doc**

Create `apps/website/content/docs/index.mdx`:

```mdx
# Angular Stream Resource Documentation

Welcome to the Angular Stream Resource documentation. Run `npm run generate-docs` to
generate the full narrative documentation from the library source.

## Quick Start

Install the library:

\`\`\`bash
npm install stream-resource
\`\`\`

See the [API Reference](/api-reference) for the complete API.
```

- [ ] **Step 6: Verify build**

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 7: Commit**

```bash
git add apps/website/lib/docs.ts apps/website/components/docs/ apps/website/app/docs/ apps/website/content/docs/index.mdx
git commit -m "feat(website): add MDX docs infrastructure (MdxRenderer, DocsSidebar, docs page)"
```

---

## Task 10: API Reference Page

**Files:**
- Create: `apps/website/components/docs/ApiRefTable.tsx`
- Create: `apps/website/app/api-reference/page.tsx`

- [ ] **Step 1: Create ApiRefTable component**

Create `apps/website/components/docs/ApiRefTable.tsx`:

```tsx
interface ApiEntry {
  name: string;
  type: string;
  description: string;
}

interface ApiRefTableProps {
  title: string;
  entries: ApiEntry[];
}

export function ApiRefTable({ title, entries }: ApiRefTableProps) {
  return (
    <section className="mb-12">
      <h2 className="font-heading font-bold text-2xl mb-6">{title}</h2>
      <div className="rounded border border-gold-faint overflow-hidden">
        <table className="w-full text-sm">
          <thead>
            <tr className="border-b border-gold-faint bg-white/5">
              <th className="text-left px-4 py-3 text-gold font-mono font-medium">Name</th>
              <th className="text-left px-4 py-3 text-text-secondary font-medium">Type</th>
              <th className="text-left px-4 py-3 text-text-secondary font-medium">Description</th>
            </tr>
          </thead>
          <tbody>
            {entries.map((e, i) => (
              <tr key={e.name} className={i % 2 === 0 ? '' : 'bg-white/5'}>
                <td className="px-4 py-3 font-mono text-gold text-xs">{e.name}</td>
                <td className="px-4 py-3 font-mono text-text-muted text-xs">{e.type}</td>
                <td className="px-4 py-3 text-text-secondary">{e.description}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Create API reference page**

Create `apps/website/app/api-reference/page.tsx`:

```tsx
import { ApiRefTable } from '../../components/docs/ApiRefTable';

export const metadata = { title: 'API Reference — stream-resource' };

const streamResourceOptions = [
  { name: 'apiUrl', type: 'string', description: 'LangGraph Platform base URL' },
  { name: 'assistantId', type: 'string', description: 'Agent/graph identifier' },
  { name: 'threadId', type: 'Signal<string | null> | string | null', description: 'Thread to connect to. Accepts Angular Signal for reactive updates.' },
  { name: 'onThreadId', type: '(id: string) => void', description: 'Callback when a new thread is created' },
  { name: 'initialValues', type: 'Partial<T>', description: 'Pre-populate state before first stream' },
  { name: 'messagesKey', type: 'string', description: 'State key holding messages (default: "messages")' },
  { name: 'throttle', type: 'number | false', description: 'Throttle rapid updates in ms. false disables throttling.' },
  { name: 'toMessage', type: '(msg) => BaseMessage', description: 'Custom message class converter' },
  { name: 'transport', type: 'StreamResourceTransport', description: 'Custom transport, replaces default FetchStreamTransport' },
];

const streamResourceRef = [
  { name: 'value()', type: 'Signal<T>', description: 'Current state values' },
  { name: 'status()', type: 'Signal<ResourceStatus>', description: 'idle | loading | resolved | error' },
  { name: 'isLoading()', type: 'Signal<boolean>', description: 'Derived: status === loading' },
  { name: 'error()', type: 'Signal<unknown>', description: 'Last error or undefined' },
  { name: 'hasValue()', type: 'Signal<boolean>', description: 'true once values received at least once' },
  { name: 'messages()', type: 'Signal<BaseMessage[]>', description: 'Streaming messages array' },
  { name: 'interrupt()', type: 'Signal<Interrupt | undefined>', description: 'Current interrupt state' },
  { name: 'interrupts()', type: 'Signal<Interrupt[]>', description: 'All interrupts in the current run' },
  { name: 'toolProgress()', type: 'Signal<ToolProgress[]>', description: 'Tool execution progress events' },
  { name: 'toolCalls()', type: 'Signal<ToolCallWithResult[]>', description: 'Tool calls with results' },
  { name: 'branch()', type: 'Signal<string>', description: 'Current thread branch' },
  { name: 'history()', type: 'Signal<ThreadState<T>[]>', description: 'Thread state history' },
  { name: 'isThreadLoading()', type: 'Signal<boolean>', description: 'true while loading thread history' },
  { name: 'subagents()', type: 'Signal<Map<string, SubagentStreamRef>>', description: 'Subagent streams map' },
  { name: 'activeSubagents()', type: 'Signal<SubagentStreamRef[]>', description: 'Currently active subagents' },
  { name: 'submit(values, opts?)', type: 'Promise<void>', description: 'Submit values to the agent' },
  { name: 'stop()', type: 'Promise<void>', description: 'Abort the current stream' },
  { name: 'reload()', type: 'void', description: 'Re-submit the last values' },
  { name: 'switchThread(threadId)', type: 'void', description: 'Switch to a different thread' },
  { name: 'joinStream(runId, lastEventId?)', type: 'Promise<void>', description: 'Join an existing run stream' },
  { name: 'setBranch(branch)', type: 'void', description: 'Switch to a named branch' },
];

export default function ApiReferencePage() {
  return (
    <div className="mx-auto max-w-5xl px-6 py-16">
      <h1 className="font-heading font-extrabold mb-4" style={{ fontSize: 'clamp(36px, 5vw, 60px)' }}>
        API <span className="text-gold italic">Reference</span>
      </h1>
      <p className="text-text-secondary mb-12">Complete API surface for stream-resource.</p>
      <ApiRefTable title="streamResource() Options" entries={streamResourceOptions} />
      <ApiRefTable title="StreamResourceRef Members" entries={streamResourceRef} />
    </div>
  );
}
```

- [ ] **Step 3: Verify build**

```bash
npx nx build website
```

Expected: Build succeeds

- [ ] **Step 4: Commit**

```bash
git add apps/website/components/docs/ApiRefTable.tsx apps/website/app/api-reference/
git commit -m "feat(website): add API reference page"
```

---

## Task 11: Documentation Generation Scripts

**Files:**
- Create: `apps/website/scripts/generate-api-docs.ts`
- Create: `apps/website/scripts/generate-narrative-docs.ts`
- Modify: `apps/website/package.json` (add `generate-docs` script)

- [ ] **Step 1: Create TypeDoc API extraction script**

Create `apps/website/scripts/generate-api-docs.ts`:

```typescript
#!/usr/bin/env tsx
import * as TypeDoc from 'typedoc';
import * as fs from 'fs';
import * as path from 'path';

const OUTPUT = path.join(__dirname, '../content/api-docs.json');

async function main() {
  const app = await TypeDoc.Application.bootstrapWithPlugins({
    entryPoints: [path.join(__dirname, '../../../libs/stream-resource/src/public-api.ts')],
    tsconfig: path.join(__dirname, '../../../libs/stream-resource/tsconfig.lib.json'),
    json: OUTPUT,
    skipErrorChecking: true,
  });

  const project = await app.convert();
  if (!project) throw new Error('TypeDoc failed to convert');

  await app.generateJson(project, OUTPUT);
  console.log(`[generate-api-docs] Written to ${OUTPUT}`);
}

main().catch(console.error);
```

- [ ] **Step 2: Create Claude narrative docs generation script**

Create `apps/website/scripts/generate-narrative-docs.ts`:

```typescript
#!/usr/bin/env tsx
import Anthropic from '@anthropic-ai/sdk';
import * as fs from 'fs';
import * as path from 'path';

const MODEL = process.env['ANTHROPIC_MODEL'] ?? 'claude-sonnet-4-6';
const DOCS_DIR = path.join(__dirname, '../content/docs');
const API_DOCS = path.join(__dirname, '../content/api-docs.json');

const PAGES = [
  { slug: 'getting-started', prompt: 'Write a Getting Started guide for stream-resource: installation, peer dependencies, first component, and provideStreamResource() setup. Target: senior Angular engineer who knows LangChain.' },
  { slug: 'stream-resource-fn', prompt: 'Write a deep-dive API guide for the streamResource() function: all options, the StreamResourceRef surface, signal lifecycle, and injection context requirements.' },
  { slug: 'testing', prompt: 'Write a Testing guide for stream-resource using MockStreamTransport: unit tests with Vitest, test harness setup, scripted event sequences, and error simulation patterns.' },
  { slug: 'transport', prompt: 'Write a Transport guide for stream-resource: FetchStreamTransport, custom transport implementation, auth header injection patterns, and onThreadId usage.' },
  { slug: 'limitations', prompt: 'Write a Limitations guide explaining the behavioral differences between streamResource() and React useStream(): concurrent-mode batching, Server Components, and the v1 deferred features (toolProgress, getMessagesMetadata).' },
];

async function main() {
  const client = new Anthropic();
  const apiDocs = fs.existsSync(API_DOCS)
    ? JSON.parse(fs.readFileSync(API_DOCS, 'utf-8'))
    : {};

  fs.mkdirSync(DOCS_DIR, { recursive: true });

  for (const page of PAGES) {
    console.log(`[generate-narrative-docs] Generating ${page.slug}...`);
    const response = await client.messages.create({
      model: MODEL,
      max_tokens: 4096,
      messages: [{
        role: 'user',
        content: `You are a technical writer creating documentation for stream-resource, an Angular 20+ library.

API shape (TypeDoc JSON summary):
${JSON.stringify(apiDocs).slice(0, 8000)}

Task: ${page.prompt}

Output: MDX format. Start with a frontmatter block:
---
title: [title]
---

Write clearly and concisely. Include TypeScript code examples. Target senior Angular engineers.`,
      }],
    });

    const content = response.content[0].type === 'text' ? response.content[0].text : '';
    fs.writeFileSync(path.join(DOCS_DIR, `${page.slug}.mdx`), content);
    console.log(`[generate-narrative-docs] ✓ ${page.slug}.mdx`);
  }
}

main().catch(console.error);
```

- [ ] **Step 3: Add generate-docs script to package.json**

Add to `apps/website/package.json` scripts:

```json
{
  "scripts": {
    "generate-docs": "npx tsx scripts/generate-api-docs.ts && npx tsx scripts/generate-narrative-docs.ts"
  }
}
```

Install `tsx`:
```bash
npm install -D tsx
```

- [ ] **Step 4: Verify scripts compile**

```bash
cd apps/website && npx tsc --noEmit --skipLibCheck scripts/generate-api-docs.ts
cd apps/website && npx tsc --noEmit --skipLibCheck scripts/generate-narrative-docs.ts
```

Expected: No type errors

- [ ] **Step 5: Commit**

```bash
git add apps/website/scripts/ apps/website/package.json
git commit -m "feat(website): add TypeDoc + Claude API documentation generation scripts"
```

---

## Task 12: Angular Elements Demo App

**Files:**
- Create: `apps/demo/` (generated by Nx)
- Create: `apps/demo/src/app/chat-demo/chat-demo.component.ts`
- Create: `apps/demo/src/app/chat-demo/chat-demo.module.ts`
- Create: `apps/demo/src/main.ts`

- [ ] **Step 1: Generate Angular Elements demo app**

```bash
npx nx g @nx/angular:app demo \
  --directory=apps/demo \
  --style=css \
  --routing=false \
  --standalone=false \
  --no-interactive
```

- [ ] **Step 2: Create ChatDemo web component**

Create `apps/demo/src/app/chat-demo/chat-demo.component.ts`:

```typescript
import { Component, Input, OnInit, ViewEncapsulation, Injector, runInInjectionContext } from '@angular/core';
import { CommonModule } from '@angular/common';
import { streamResource } from 'stream-resource';
import type { BaseMessage } from '@langchain/core/messages';

@Component({
  selector: 'stream-chat-demo',
  standalone: true,
  imports: [CommonModule],
  encapsulation: ViewEncapsulation.ShadowDom,
  template: `
    <div class="demo-container">
      <div class="messages">
        @for (msg of stream.messages(); track msg.id) {
          <div [class]="'message ' + msg.type">
            <span class="label">{{ msg.type }}</span>
            <p>{{ msg.content }}</p>
          </div>
        }
        @if (stream.isLoading()) {
          <div class="message ai loading">
            <span class="label">ai</span>
            <p>Thinking…</p>
          </div>
        }
        @if (!stream.messages().length && !stream.isLoading()) {
          <p class="placeholder">Send a message to start streaming.</p>
        }
      </div>
      <form (submit)="send($event)" class="input-row">
        <input #inp placeholder="Ask the agent something…" class="input" />
        <button type="submit" [disabled]="stream.isLoading()" class="send-btn">
          {{ stream.isLoading() ? '…' : 'Send' }}
        </button>
      </form>
    </div>
  `,
  styles: [`
    :host { display: block; font-family: Inter, system-ui, sans-serif; }
    .demo-container { background: #0d0b09; border: 1px solid rgba(212,170,106,0.2); border-radius: 8px; padding: 20px; max-width: 640px; }
    .messages { min-height: 200px; max-height: 360px; overflow-y: auto; margin-bottom: 16px; display: flex; flex-direction: column; gap: 8px; }
    .message { padding: 10px 14px; border-radius: 6px; }
    .message.human { background: rgba(212,170,106,0.1); border: 1px solid rgba(212,170,106,0.2); }
    .message.ai { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08); }
    .label { font-family: 'JetBrains Mono', monospace; font-size: 10px; text-transform: uppercase; color: #7a6e5e; }
    p { color: #f4efe6; font-size: 14px; margin: 4px 0 0; }
    .placeholder { color: #7a6e5e; font-size: 14px; text-align: center; padding: 32px; }
    .input-row { display: flex; gap: 8px; }
    .input { flex: 1; background: rgba(255,255,255,0.05); border: 1px solid rgba(212,170,106,0.2); border-radius: 6px; padding: 10px 14px; color: #f4efe6; font-size: 14px; outline: none; }
    .send-btn { background: #d4aa6a; color: #080705; border: none; border-radius: 6px; padding: 10px 20px; font-size: 14px; font-weight: 600; cursor: pointer; }
    .send-btn:disabled { opacity: 0.5; cursor: not-allowed; }
  `],
})
export class ChatDemoComponent implements OnInit {
  @Input() apiUrl = '';
  @Input() assistantId = 'agent';

  stream!: ReturnType<typeof streamResource>;

  // Injector is stored so we can call streamResource() in runInInjectionContext.
  // streamResource() MUST be called in an injection context. @Input() values
  // aren't resolved until ngOnInit, so we cannot call it in the constructor.
  constructor(private injector: Injector) {}

  ngOnInit() {
    // runInInjectionContext re-establishes the injection context so that
    // streamResource() can call inject(DestroyRef) and toSignal() internally.
    this.stream = runInInjectionContext(this.injector, () =>
      streamResource({
        apiUrl: this.apiUrl,
        assistantId: this.assistantId,
      })
    );
  }

  send(e: Event) {
    e.preventDefault();
    const form = e.target as HTMLFormElement;
    const input = form.querySelector('input') as HTMLInputElement;
    const content = input.value.trim();
    if (!content) return;
    this.stream.submit({ messages: [{ role: 'human', content }] });
    input.value = '';
  }
}
```

- [ ] **Step 3: Create Angular Elements bootstrap**

Create `apps/demo/src/main.ts`:

```typescript
import { createApplication } from '@angular/platform-browser';
import { createCustomElement } from '@angular/elements';
import { ChatDemoComponent } from './app/chat-demo/chat-demo.component';

(async () => {
  const app = await createApplication();
  const ChatDemo = createCustomElement(ChatDemoComponent, { injector: app.injector });
  customElements.define('stream-chat-demo', ChatDemo);
})();
```

- [ ] **Step 4: Add demo build target to copy output to website/public/demo/**

In `apps/demo/project.json`, add a post-build copy target (or configure `outputPath` to `apps/website/public/demo/`). The simplest approach:

```json
{
  "targets": {
    "build": {
      "executor": "@nx/angular:browser-esbuild",
      "options": {
        "outputPath": "apps/website/public/demo",
        "index": "apps/demo/src/index.html",
        "main": "apps/demo/src/main.ts",
        "outputHashing": "none"
      }
    }
  }
}
```

- [ ] **Step 5: Verify demo builds**

```bash
npx nx build demo
```

Expected: Angular Elements bundle written to `apps/website/public/demo/`

- [ ] **Step 6: Commit**

```bash
git add apps/demo/
git commit -m "feat(demo): add Angular Elements chat demo component"
```

---

## Task 13: Playwright E2E Tests

**Files:**
- Create: `apps/website/e2e/landing.spec.ts`
- Create: `apps/website/e2e/pricing.spec.ts`
- Create: `apps/website/e2e/docs.spec.ts`
- Create: `apps/website/playwright.config.ts`

- [ ] **Step 1: Write Playwright config**

Create `apps/website/playwright.config.ts`:

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  use: { baseURL: 'http://localhost:3000' },
  webServer: {
    command: 'npx nx serve website',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env['CI'],
  },
  projects: [
    { name: 'desktop', use: { ...devices['Desktop Chrome'], viewport: { width: 1280, height: 800 } } },
    { name: 'mobile', use: { ...devices['iPhone 12'] } },
    { name: 'tablet', use: { ...devices['iPad (gen 7)'] } },
  ],
});
```

- [ ] **Step 2: Write landing page tests**

Create `apps/website/e2e/landing.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Landing page', () => {
  test('renders hero headline', async ({ page }) => {
    await page.goto('/');
    await expect(page.getByText('stream')).toBeVisible();
    await expect(page.getByText('resource')).toBeVisible();
  });

  test('renders tagline', async ({ page }) => {
    await page.goto('/');
    await expect(page.getByText('The Enterprise Streaming Resource for LangChain and Angular')).toBeVisible();
  });

  test('renders architecture diagram SVG', async ({ page }) => {
    await page.goto('/');
    await expect(page.locator('svg[aria-label="Angular Stream Resource architecture diagram"]')).toBeVisible();
  });

  test('renders install command', async ({ page }) => {
    await page.goto('/');
    await expect(page.getByText('npm install stream-resource')).toBeVisible();
  });

  test('renders feature cards', async ({ page }) => {
    await page.goto('/');
    await expect(page.getByText('Angular Signals')).toBeVisible();
    await expect(page.getByText('Full Parity')).toBeVisible();
    await expect(page.getByText('RxJS Core')).toBeVisible();
  });

  test('mobile: hero is readable at 375px', async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 667 });
    await page.goto('/');
    await expect(page.getByText('npm install stream-resource')).toBeVisible();
  });
});
```

- [ ] **Step 3: Write pricing page tests**

Create `apps/website/e2e/pricing.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Pricing page', () => {
  test('shows all three plans', async ({ page }) => {
    await page.goto('/pricing');
    await expect(page.getByText('Open Source')).toBeVisible();
    await expect(page.getByText('Developer Seat')).toBeVisible();
    await expect(page.getByText('App Deployment')).toBeVisible();
  });

  test('shows enterprise section', async ({ page }) => {
    await page.goto('/pricing');
    await expect(page.getByText('Enterprise')).toBeVisible();
    await expect(page.getByText('Contact Sales')).toBeVisible();
  });

  test('lead form requires name and email', async ({ page }) => {
    await page.goto('/pricing');
    await page.click('button[type="submit"]');
    // HTML5 validation should prevent submission
    const nameInput = page.locator('input[name="name"]');
    await expect(nameInput).toBeFocused();
  });

  test('lead form submits successfully', async ({ page }) => {
    await page.goto('/pricing');
    await page.fill('input[name="name"]', 'Test User');
    await page.fill('input[name="email"]', 'test@example.com');
    await page.fill('input[name="company"]', 'Test Corp');
    await page.click('button[type="submit"]');
    await expect(page.getByText('Thank you.')).toBeVisible({ timeout: 5000 });
  });
});
```

- [ ] **Step 4: Write docs page tests**

Create `apps/website/e2e/docs.spec.ts`:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Docs page', () => {
  test('renders docs index', async ({ page }) => {
    await page.goto('/docs');
    await expect(page.getByText('Angular Stream Resource Documentation')).toBeVisible();
  });

  test('API reference renders signal table', async ({ page }) => {
    await page.goto('/api-reference');
    await expect(page.getByText('streamResource() Options')).toBeVisible();
    await expect(page.getByText('StreamResourceRef Members')).toBeVisible();
    await expect(page.getByText('assistantId')).toBeVisible();
  });

  test('Nav links are visible', async ({ page }) => {
    await page.goto('/');
    await expect(page.getByRole('link', { name: 'Docs' })).toBeVisible();
    await expect(page.getByRole('link', { name: 'Pricing' })).toBeVisible();
  });
});
```

- [ ] **Step 5: Run E2E tests**

```bash
npx nx build website && npx playwright test --config=apps/website/playwright.config.ts
```

Expected: All tests pass

- [ ] **Step 6: Commit**

```bash
git add apps/website/e2e/ apps/website/playwright.config.ts
git commit -m "test(website): add Playwright E2E tests for landing, pricing, docs"
```

---

## Task 14: Vercel Deployment Config

**Files:**
- Create: `apps/website/vercel.json`
- Create: `.env.example`

- [ ] **Step 1: Create Vercel config**

Create `apps/website/vercel.json`:

```json
{
  "buildCommand": "cd ../.. && npx nx build website",
  "outputDirectory": "apps/website/.next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "ANTHROPIC_MODEL": "claude-sonnet-4-6"
  }
}
```

- [ ] **Step 2: Create .env.example**

Create `.env.example` in the monorepo root:

```bash
# LangGraph Platform API URL for the live demo
NEXT_PUBLIC_LANGGRAPH_API_URL=https://your-deployment.us.langgraph.app

# Claude model for narrative docs generation (optional, defaults to claude-sonnet-4-6)
ANTHROPIC_MODEL=claude-sonnet-4-6

# Anthropic API key (required for npm run generate-docs)
ANTHROPIC_API_KEY=sk-ant-...
```

- [ ] **Step 3: Final build verification**

```bash
npx nx build website
npx nx build demo
```

Expected: Both build successfully

- [ ] **Step 4: Commit**

```bash
git add apps/website/vercel.json .env.example
git commit -m "chore: add Vercel deployment config and .env.example"
```

---
